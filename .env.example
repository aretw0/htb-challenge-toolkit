# Exemplo de arquivo .env para configurar o ambiente do Docker Compose.
# Copie este arquivo para .env e personalize.

# Prioridade 1: Defina o caminho para o arquivo .ovpn (relativo à raiz do projeto).
# Exemplo: OVPN_CONFIG_FILE=challenges/cap/cap.ovpn
# OVPN_CONFIG_FILE=

# Prioridade 2: Defina o nome do desafio para carregar o arquivo .ovpn específico (ex: cap).
# Se não definido, o ambiente tentará usar global.ovpn na raiz do projeto.
# CHALLENGE_NAME=
